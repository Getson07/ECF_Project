<form method="post" action="{{ path('app_reserved_table_delete', {'id': reserved_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reserved_table.id) }}">
    <button class="btn">Delete</button>
</form>
