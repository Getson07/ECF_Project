{% extends 'base.html.twig' %}

{% block title %}DishCategory{% endblock %}

{% block body %}
    <h1>DishCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dish_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dish_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dish_category_index') }}">back to list</a>

    <a href="{{ path('app_dish_category_edit', {'id': dish_category.id}) }}">edit</a>

    {{ include('dish_category/_delete_form.html.twig') }}
{% endblock %}
