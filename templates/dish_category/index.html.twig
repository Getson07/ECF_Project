{% extends 'base.html.twig' %}

{% block title %}DishCategory index{% endblock %}

{% block body %}
    <h1>DishCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dish_category in dish_categories %}
            <tr>
                <td>{{ dish_category.id }}</td>
                <td>{{ dish_category.name }}</td>
                <td>
                    <a href="{{ path('app_dish_category_show', {'id': dish_category.id}) }}">show</a>
                    <a href="{{ path('app_dish_category_edit', {'id': dish_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_dish_category_new') }}">Create new</a>
{% endblock %}
